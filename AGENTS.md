# i CUBE TEST 作業進捗の共通運用

5分以上のGate、複数工程QA、調査→実装→テストでは、日本語のProgress Logを使用する。

- 開始時に工程を番号付きで示す。各工程の状態は PENDING / IN PROGRESS / COMPLETE / BLOCKED / FAILED。
- 主要工程の切り替わりと長時間処理中に、3〜8行程度で現在・完了・残作業を伝える。細かい操作実況は不要。
- Reproduce → Root Cause → Minimum Fix → Targeted Test → Regression の順序を守る。
- 問題やブロッカーは隠さず、確定した原因、対応、必要なユーザー判断を簡潔に示す。
- テスト・ブラウザ確認が要求されている場合、実装だけでCOMPLETEにしない。
- Browser emulationとPhysical deviceの結果を必ず分離する。未確認をPASSと報告しない。
- 終了時に工程を閉じ、変更・原因・テスト・ブラウザQA・未完了項目を報告する。
- 権限追加、課金、正式素材不足、権利不明、危険な本番変更、仕様矛盾で判断が必要なら停止して方向性を確認する。

この運用はユーザーの2026-09-12共通指示に基づく。最新のユーザー指示と上位指示を優先する。
